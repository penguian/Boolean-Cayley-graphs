# tox -c tox-passagemath.ini
[tox]
envlist = passagemath

[testenv:passagemath]
usedevelop = True
extras = passagemath, psycopg2-binary

setenv =
    # For access to _doctest_environment.py
    PYTHONPATH=.

commands =
    sage -tp --environment=_doctest_environment boolean_cayley_graphs
